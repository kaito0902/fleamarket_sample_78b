-# 出品アイコン---------------------------------------------
.displayIcon
  - if user_signed_in?
    = link_to new_product_path do
      %span.displayIcon__text
        出品する
      = image_tag "icon/icon_camera.png"
  - else
    = link_to new_user_session_path  do
      %span.displayIcon__text
        出品する
      = image_tag "icon/icon_camera.png"